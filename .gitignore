.DS_Store

# Sidecars this tool creates, so a run inside the repo leaves nothing to commit.
*.sha256

# Go build and test artifacts. The draft binary is built locally and from CI;
# it is not a tracked artifact.
go/treecheck-go
*.test
*.out
coverage.*
